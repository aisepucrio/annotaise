type ContextVizualizerProps = {
  text?: string;
};

export default function ContextVizualizer({ text }: ContextVizualizerProps) {
  // Placeholder para futura visualização de contextos.
  void text;
  return null;
}
